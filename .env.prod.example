# Template for the server-managed .env used by docker-compose.prod.yml.
# Copy to .env on the server and fill with generated secrets. NEVER commit .env.
#   openssl rand -hex 32   # generate each secret
DB_PASSWORD=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
# Platform super-admin (provisions company accounts at /platform).
# Bootstrapped on api start if it does not exist yet.
PLATFORM_ADMIN_USERNAME=
PLATFORM_ADMIN_PASSWORD=
# Column-level PII encryption key — 32 bytes as 64 hex chars (openssl rand -hex 32).
# Encrypts user emails + supplier contact details at rest. If lost, those
# encrypted values are unrecoverable. Leave blank to disable (passthrough).
FIELD_ENCRYPTION_KEY=
CORS_ORIGINS=https://ims.urbanwerkzsg.com
# RATE_LIMIT_MAX=300
# DEFAULT_BASE_CURRENCY=USD
# WRITE_OFF_APPROVAL_THRESHOLD=500
# ALLOW_NEGATIVE_STOCK=false
